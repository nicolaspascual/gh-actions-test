name: "Build"
on: [push, pull_request]
jobs: 
  build:
    runs-on: ubuntu-18.04
    steps:
      - name: Checkout
        uses: actions/checkout@v1
      - name: List Files
        uses: jcblw/bash@1.0.0
        with:
          args: ls -la
